import 'package:flutter/material.dart';

class UnitsScreenMobile extends StatelessWidget {
  const UnitsScreenMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}